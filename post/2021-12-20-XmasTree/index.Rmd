
---
title: Xmas Tree in R
author: Christian Saal
date: '`r Sys.Date()`'
slug: []
categories: []
tags: [R]
draft: false
---

```{r setup, include=FALSE}

knitr::opts_chunk$set(dpi=300, cache = TRUE, comment = NA)
knitr::opts_knit$set(global.par = TRUE)

```


![](/XmasTree/XmasTree.gif)


```{r}
 # t sin(t) Xmas tree, see https://community.wolfram.com/groups/-/m/t/175891

library(rgl)

t <- seq(0, 100, by = 0.7)^0.6
x <- t * c(sin(t), sin(t + pi))
y <- t * c(cos(t), cos(t + pi))
z <- -2 * c(t, t)
color <- rep(c("darkgreen", "gold"), each = length(t))

open3d(windowRect = c(100, 100, 600, 600), zoom = 0.9)
bg3d("black")
spheres3d(x, y, z, radius = 0.3, color = color)

# Export (requires 3rd party tool "ImageMagick" resp. magick-package)
#  movie3d(spin3d(axis = c(0, 0, 1), rpm = 4), duration = 30, dir = getwd())
```

I found that on 
[R-Bloggers](https://www.r-bloggers.com/2021/12/x-mas-tree-with-10-lines-of-r-code/?utm_source=phpList&utm_medium=email&utm_campaign=R-bloggers-daily&utm_content=HTML). 

MERRY CHRISTMAS!!
